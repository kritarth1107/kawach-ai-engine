services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kawach_ai
    ports:
      - "5433:5432"
    volumes:
      - kawach_pg:/var/lib/postgresql/data

volumes:
  kawach_pg:
